apiVersion: v1
kind: Service
metadata:
  name: my-service-nodeport
  labels:
    env: test
spec:
  type: nodeport
  ports:
   - nodePort: 30001
     port: 80
     targetPort: 80
  selector:
    env: test
